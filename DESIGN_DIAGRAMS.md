# Education Platform - Data Flow & Architecture Diagram

## 🎯 Hierarchical Domain Model

**IMPORTANT**: Academic Year = Student's Year Level (1st, 2nd, 3rd, or 4th Year)

```
┌─────────────────────────────────────────────────────────────────┐
│          ACADEMIC YEAR: SECOND_YEAR (2nd Year Students)         │
└─────────────────────────────────────────────────────────────────┘
         │                          │                    │
         ├─ PROGRAMMING LANGUAGES   │                 STUDENTS
         │  └─ Java                 │                    │
         │     └─ Challenges        │                    ├─ John Doe (2nd Yr)
         │        └─ Two Sum        │                    ├─ Jane Smith (2nd Yr)
         │        └─ Reverse String │                    └─ Bob Wilson (2nd Yr)
         │        └─ Binary Search  │
         │                          │
         │  └─ Python              │
         │     └─ Challenges       │
         │        └─ Arrays         │
         │        └─ Strings        │
         │                          │
         └─ C++                     │
            └─ Challenges           │
               └─ Sorting           │
               └─ Linked List       │
```

## 📊 Data Model Relationships

```
┌──────────────────┐         ┌─────────────────────────┐
│ AcademicYear     │ 1     N │ ProgrammingLanguage     │
│─────────────────│◄────────┤──────────────────────────│
│ id              │ year_id │ id                       │
│ year            │         │ name                     │
│ createdAt       │         │ academicYear_id          │
└──────────────────┘         └─────────────────────────┘
         │                             │
         │ 1                  N        │ 1
         │                    ┌────────┘
         │                    │
         │                    ▼
         │          ┌──────────────────────┐
         │          │ Challenges           │
         │          │──────────────────────│
         │          │ id                   │
         │          │ title                │
         │          │ description          │
         │          │ difficultyLevel      │
         │          │ timeLimit            │
         │          │ memoryLimit          │
         │          │ testCases            │
         │          │ isActive             │
         │          │ createdAt            │
         │          │ programmingLanguage_ │
         │          │ id                   │
         │          └──────────────────────┘
         │                    │
         │                    │
         │ N              ┌────┴────┐
         │                │         │
    ┌────▼──────────────┐ │    ┌────▼──────────────────┐
    │ Student           │ │    │ StudentSubmission     │
    │──────────────────│ │    │──────────────────────┤
    │ id               │ │    │ id                   │
    │ name             │ │    │ code                 │
    │ email            │ │    │ status               │
    │ rollNumber       │ │    │ submittedAt          │
    │ enrolledAt       │ │    │ executionTimeMs      │
    │ lastActiveAt     │ │    │ testOutput           │
    │ totalSolved      │ │    │ student_id  ◄────────┘
    │ academicYear_id  │ │    │ challenge_id ◄───────┐
    └────┬─────────────┘ │    └──────────────────────┘
         │               │
         │        ┌──────┴──────────────────┐
         │        │ StudentChallengeProgress│
         │        │────────────────────────│
         │        │ id                     │
         │        │ attemptCount           │
         │        │ isSolved               │
         │        │ firstAttemptAt         │
         │        │ solvedAt               │
         │        │ solvingTimeMinutes     │
         │        │ student_id ◄───────────┘
         │        │ challenge_id ◄─────────┐
         │        └────────────────────────┘
         │
         └─ (1 to N relationship)
```

## 🔄 Complete Student Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                  STUDENT PLATFORM WORKFLOW                      │
└─────────────────────────────────────────────────────────────────┘

1. ENROLLMENT
   ┌──────────────┐
   │ Student      │
   │ Enrollment   │  ──POST /api/students──>  ┌──────────────┐
   │              │                           │ StudentService
   │ Name         │                           │ (addStudent) │
   │ Email        │◄──200 OK──────────────────┤              │
   │ Academic Yr  │                           └──────────────┘
   └──────────────┘

2. VIEW CURRICULUM
   ┌──────────────┐
   │ Student      │
   │              │  ──GET /api/students/{id}/curriculum──>  ┌──────────────┐
   │              │                                          │ StudentService
   │              │◄──200 OK──────────────────────────────────┤              │
   │ Languages    │  [Java, Python, C++]                    │ (getCurriculum)
   │ Challenges   │                                          └──────────────┘
   └──────────────┘

3. BROWSE CHALLENGES
   ┌──────────────┐
   │ View Langs   │  ──GET /api/languages/year/{yearId}──>  ┌──────────────┐
   │              │                                          │ LanguageService
   │ Select Lang  │◄──200 OK──────────────────────────────────┤              │
   │              │  [Python, Java, C++]                     │ (getByYear)  │
   │              │                                          └──────────────┘
   └──────────────┘
            │
            │ Select Language
            ▼
   ┌──────────────┐
   │ View Challs  │  ──GET /api/challenges/language/{id}──>  ┌──────────────┐
   │              │                                          │ ChallengesService
   │ Challenge    │◄──200 OK──────────────────────────────────┤              │
   │ List         │  [Two Sum, Reverse String, ...]         │ (getByLang)  │
   │              │                                          └──────────────┘
   └──────────────┘

4. SOLVE CHALLENGE
   ┌──────────────┐
   │ Solve Chall  │
   │              │
   │ View problem │
   │ Write code   │
   │              │
   └──────────────┘
            │
            │ Click Submit
            ▼
   ┌──────────────────────────────────────────────┐
   │ Code Submission                              │
   │                                              │
   │ POST /api/submissions/student/{sid}/         │
   │       challenge/{cid}                        │
   │                                              │
   │ {                                            │
   │   submittedCode: "...",                      │
   │   status: "SUBMITTED",                       │
   │   language: "Python"                         │
   │ }                                            │
   └──────────────────────────────────────────────┘
            │
            ▼
   ┌──────────────────────────────────────────────┐
   │ StudentSubmissionService                     │
   │ • Save Submission                            │
   │ • Update Progress Tracking                   │
   │ • Update Challenge Stats                     │
   └──────────────────────────────────────────────┘
            │
            ├─ StudentSubmission (store code)
            ├─ StudentChallengeProgress (update stats)
            └─ Challenge (increment counters)
            │
            ▼
   ┌──────────────────────────────────────────────┐
   │ Response (201 Created)                       │
   │ {                                            │
   │   id: 1,                                     │
   │   status: "ACCEPTED",                        │
   │   submittedAt: "2024-02-03T10:35:00"        │
   │ }                                            │
   └──────────────────────────────────────────────┘

5. VIEW STATISTICS
   ┌──────────────┐
   │ Student      │
   │ Dashboard    │  ──GET /api/submissions/progress/──>  ┌──────────────┐
   │              │       student/{id}/solved-count       │ SubmissionSvc
   │ Total Solved │◄──200 OK──────────────────────────────┤              │
   │ Accuracy %   │  { totalSolved: 10 }                 │ (getStats)   │
   │ Time Spent   │                                       └──────────────┘
   └──────────────┘
```

## 🗄️ Database Schema Diagram

```
┌─────────────────────┐
│  academic_year      │
├─────────────────────┤
│ id (PK)             │
│ academic_year_name  │
│ created_at          │
└──────┬──────────────┘
       │
       │ 1:N
       │
       ▼
┌─────────────────────────────┐
│  programming_language       │
├─────────────────────────────┤
│ id (PK)                     │
│ name                        │
│ academic_year_id (FK)       │
└──────┬──────────────────────┘
       │
       │ 1:N
       │
       ▼
┌──────────────────────────────┐
│  challenges                  │
├──────────────────────────────┤
│ id (PK)                      │
│ title                        │
│ description                  │
│ problem_statement            │
│ difficulty_level             │
│ time_limit                   │
│ memory_limit                 │
│ test_cases                   │
│ is_active                    │
│ accepted_count               │
│ total_submissions            │
│ programming_language_id (FK) │
│ created_at                   │
│ updated_at                   │
└──────┬───────────────────────┘
       │
       ├─────────────────────┬──────────────────┐
       │                     │                  │
       │ 1:N           1:N    │ 1:N              │
       │                     │                  │
       ▼                     ▼                  ▼
       │
┌──────────────────────────────────┐
│  student                         │
├──────────────────────────────────┤
│ id (PK)                          │
│ name                             │
│ email                            │
│ roll_number                      │
│ enrolled_at                      │
│ last_active_at                   │
│ total_challenges_solved          │
│ average_score                    │
│ academic_year_id (FK)            │
└──────┬──────────────────────────┘
       │
       ├──────────────┬────────────────────┐
       │              │                    │
       │ 1:N     1:N  │            1:N     │
       │              │                    │
       ▼              ▼                    ▼
┌──────────────────────────────────┐
│  student_submission              │
├──────────────────────────────────┤
│ id (PK)                          │
│ student_id (FK)                  │
│ challenge_id (FK)                │
│ submitted_code                   │
│ status                           │
│ test_output                      │
│ submitted_at                     │
│ execution_time_ms                │
│ language                         │
└──────────────────────────────────┘

┌──────────────────────────────────┐
│  student_challenge_progress      │
├──────────────────────────────────┤
│ id (PK)                          │
│ student_id (FK)                  │
│ challenge_id (FK)                │
│ attempt_count                    │
│ is_solved                        │
│ first_attempt_at                 │
│ solved_at                        │
│ solving_time_minutes             │
│ accepted_submission_id           │
└──────────────────────────────────┘
```

## 📡 API Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    REST API ENDPOINTS                       │
├──────────────────┬──────────────────┬──────────────────────┤
│ Academic Year    │ Language         │ Challenge            │
│ Controller       │ Controller       │ Controller           │
├──────────────────┼──────────────────┼──────────────────────┤
│ POST   /api/..   │ POST   /api/..   │ POST   /api/..       │
│ GET    /api/..   │ GET    /api/..   │ GET    /api/..       │
│ PUT    /api/..   │ PUT    /api/..   │ PUT    /api/..       │
│ DELETE /api/..   │ DELETE /api/..   │ DELETE /api/..       │
└──────────────────┴──────────────────┴──────────────────────┘
         │                  │                     │
         │ calls           │ calls               │ calls
         ▼                  ▼                     ▼
┌──────────────────────────────────────────────────────────────┐
│                    SERVICE LAYER                             │
├──────────────────┬──────────────────┬──────────────────────┤
│ AcademicYearSvc  │ LanguageService  │ ChallengesService    │
├──────────────────┼──────────────────┼──────────────────────┤
│ create           │ create           │ create               │
│ read             │ read             │ read                 │
│ update           │ update           │ update               │
│ delete           │ delete           │ delete               │
│ getCount         │ getByYear        │ getByLanguage        │
└──────────────────┴──────────────────┴──────────────────────┘
         │                  │                     │
         │ uses            │ uses                │ uses
         ▼                  ▼                     ▼
┌──────────────────────────────────────────────────────────────┐
│                  REPOSITORY LAYER (Data Access)              │
├──────────────────┬──────────────────┬──────────────────────┤
│ AcademicYearRepo │ LanguageRepo     │ ChallengesRepo       │
├──────────────────┼──────────────────┼──────────────────────┤
│ findAll          │ findAll          │ findAll              │
│ findById         │ findById         │ findById             │
│ save             │ save             │ save                 │
│ delete           │ delete           │ delete               │
│ findByYear       │ findByName       │ findByLanguage       │
└──────────────────┴──────────────────┴──────────────────────┘
         │                  │                     │
         │ queries         │ queries             │ queries
         ▼                  ▼                     ▼
┌──────────────────────────────────────────────────────────────┐
│                    DATABASE (MySQL/PostgreSQL)               │
└──────────────────────────────────────────────────────────────┘
```

## 🎓 Student Journey Map

```
START
  │
  ├─ Create Account
  │  └─ POST /api/students
  │     └─ System creates student in academic year
  │
  ├─ VIEW DASHBOARD
  │  ├─ GET /api/students/{id}
  │  ├─ GET /api/students/{id}/curriculum
  │  │
  │  ├─ See Academic Year Details
  │  ├─ See Available Languages
  │  └─ See Challenges per Language
  │
  ├─ EXPLORE CHALLENGES
  │  ├─ GET /api/languages/year/{yearId}
  │  ├─ GET /api/challenges/language/{langId}
  │  ├─ GET /api/challenges/difficulty/{level}
  │  │
  │  ├─ View Challenge Details
  │  ├─ Read Problem Statement
  │  ├─ Check Sample Input/Output
  │  └─ See Test Cases
  │
  ├─ SOLVE CHALLENGE
  │  ├─ Write Code
  │  ├─ POST /api/submissions/student/{sid}/challenge/{cid}
  │  │
  │  ├─ System checks:
  │  │  ├─ Compiles code
  │  │  ├─ Runs test cases
  │  │  ├─ Updates StudentSubmission
  │  │  └─ Updates StudentChallengeProgress
  │  │
  │  └─ See Result (ACCEPTED/WRONG_ANSWER/ERROR)
  │
  ├─ CHECK PROGRESS
  │  ├─ GET /api/submissions/progress/student/{id}/solved
  │  ├─ GET /api/submissions/progress/student/{id}/solved-count
  │  ├─ GET /api/submissions/progress/student/{id}/accuracy
  │  │
  │  └─ View Statistics Dashboard
  │     ├─ Problems Solved
  │     ├─ Accuracy Rate
  │     ├─ Languages Practiced
  │     └─ Most Recent Activity
  │
  └─ CONTINUE LEARNING
     └─ Repeat: Explore → Solve → Check Progress
```

## ⏱️ Request/Response Timeline

```
Client                                              Server
  │                                                   │
  ├─ POST /api/submissions/student/1/challenge/1    │
  │                                                   │
  ├─────────────────────────────────────────────►   │
  │                                        StudentSubmissionService
  │                                        • Validate student
  │                                        • Validate challenge
  │                                        • Save submission
  │                                        │
  │                                        ├─ StudentSubmissionRepository.save()
  │                                        │
  │                                        ├─ ChallengesService.incrementCount()
  │                                        │
  │                                        └─ updateProgressTracking()
  │                                           ├─ Check if solved
  │                                           ├─ Save progress
  │                                           └─ Update challenge stats
  │                                                   │
  │◄───────────────────────────────────────────────  │
  │  201 Created                                      │
  │  { id: 1, status: "ACCEPTED", ... }              │
  │                                                   │
  
Client receives response and updates UI
  │
  ├─ GET /api/submissions/progress/student/1/solved │
  │                                                   │
  ├─────────────────────────────────────────────►   │
  │                                        StudentSubmissionService
  │                                        • Query progress
  │                                        • Return solved challenges
  │                                                   │
  │◄───────────────────────────────────────────────  │
  │  200 OK                                           │
  │  [{ challenge_id: 1, solved: true, ... }, ...]   │
  │                                                   │
```

---

## Summary

This architecture enables:
- ✅ Independent resource management (Year → Language → Challenge)
- ✅ Complete student progress tracking
- ✅ Competitive programming features (like LeetCode)
- ✅ Scalable and maintainable codebase
- ✅ RESTful API design
- ✅ Transaction management
- ✅ Proper error handling
